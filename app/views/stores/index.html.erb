<h1>Stores#index</h1>
<p>Find me in app/views/stores/index.html.erb</p>

<%= Time.now %>

</br>

<%= @location %>

</br>
</br>

<%= link_to '[new store]', new_store_path %> 

</br>
</br>

<table>
  <tr> 
    <td> [ID] </td>
    <td> [Name Store] </td>
    <td> [Address] </td>
    <td> [Account] </td>
    <td> [Payment Account] </td>
    <td> [Detail] </td>
  </tr>
    <% @store.each do |store| %>
  <tr>
    <td> <%= store.id %> </td>
    <td> <%= store.name_store %> </td>
    <td> <%= store.address %> </td>
    <td> <%= store.account.name %> | {<%= store.account.id %>} </td>
    <td> <%= store.payment.account.name %> | {<%= store.payment.id %>} </td>
    <td> <%= link_to '[show]', store_path(store) %> | <%= link_to '[edit]', edit_store_path(store) %> | <%= link_to '[delete]', store_path(store), method: :delete, data: {confirm: 'are you sure?'} %> </td>
  </tr>
   <% end %>
</table>

</br>

<%= link_to '[account data]', {controller: 'accounts', action: 'index'} %>
<%= link_to '[payment data]', {controller: 'payments', action: 'index'} %>

</br>

<% if flash[:notice].present? %>
  <%= flash[:notice] %>
<% end %>

