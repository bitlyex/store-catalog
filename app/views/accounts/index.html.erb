<h1>Accounts#index</h1>
<p>Find me in app/views/accounts/index.html.erb</p>
<%= Time.now %>
</br>
<%= @location %>

</br>
</br>

<%= link_to '[new account]', new_account_path %> 

</br>
</br>

<table>
  <tr> 
    <td> [ID] </td>
    <td> [Name] </td>
    <td> [User Role] </td>
    <td> [Store] </td>
    <td> [Detail] </td>
  </tr>
    <% @account.each do |account| %>
  <tr>
    <td> <%= account.id %> </td>
    <td> <%= account.name %> </td> 
    <td> <%= account.user_role %> </td>
    <td> <%= account.stores.pluck(:name_store) %> </td>
    <td> <%= link_to '[show]', account_path(account) %> | <%= link_to '[edit]', edit_account_path(account) %> | <%= link_to '[delete]', account_path(account), method: :delete, data: {confirm: 'are you sure?'} %> </td>
  </tr>
    <% end %>
</table>

</br>

<%= link_to '[data store]', {controller: 'stores', action: 'index'} %>
<%= link_to '[payment data]', {controller: 'payments', action: 'index'} %>

</br>
<% if flash[:notice].present? %>
  <%= flash[:notice] %>
<% end %> 


